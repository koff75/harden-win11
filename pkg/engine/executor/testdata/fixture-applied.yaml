version: "1.0"

section:
  id: fixture_applied
  order: 99
  title: "Test fixture — non-compliant, action succeeds"
  description: "Test fixture qui ressort would_apply en dry, applied en apply réel."

rules:
  - id: fixture.success
    title: "Apply success path"
    description: "."
    explanation: "."
    severity: nice-to-have
    impact: "."
    requires_reboot: false
    profile_when: always
    depends_on: []
    irreversible: false
    action: ./pkg/engine/executor/testdata/stateful.action.ps1
    test: ./pkg/engine/executor/testdata/stateful.test.ps1
    undo: ./pkg/engine/executor/testdata/stateful.undo.ps1
